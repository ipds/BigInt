#include <iostream>
#include <vector>
#include <string>
#include <cmath>
#include "BigInt.h"

using namespace std;

int main() {
    BigInt foo(11);
    BigInt bar(10);
    cout<<foo-bar<<endl;
    return 0;
}